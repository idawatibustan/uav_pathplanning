If you have not installed the ros hardware simulation stacks before,
you might need install the following before you catkin_make the workspace.

sudo apt-get install ros-kinetic-hardware-interface
sudo apt-get install ros-kinetic-controller-interface
sudo apt-get install ros-kinetic-gazebo-ros-control


